---
name: Multiple Reactor Partner Transactions
on:
  schedule:
    - cron: '30 4,11 * * *'
  workflow_dispatch:
    inputs:
      notification:
        required: false
        description: Notification
        type: boolean
        default: true

permissions:
  id-token: write
  contents: read
  security-events: write

jobs:
  prime_to_nexus:
    name: Prime to Nexus
    uses: ./.github/workflows/single-transaction.yml
    if: success() || failure()
    with:
      bridge: Reactor
      deployment: Partner
      source_chain: Prime
      source_token: AP3X
      destination_chain: Nexus
      destination_token: AP3X
      amount: 5
      eternl_wallet_extension: Stable
      notification: false
    secrets:
      SIGN_KEY: ${{ secrets.SIGN_KEY }}
      PRIME_WALLET_RECOVERY_PHRASE: ${{ secrets.PRIME_WALLET_RECOVERY_PHRASE }}
      NEXUS_WALLET_RECOVERY_PHRASE: ${{ secrets.NEXUS_WALLET_RECOVERY_PHRASE }}
  nexus_to_prime:
    name: Nexus to Prime
    uses: ./.github/workflows/single-transaction.yml
    with:
      bridge: Reactor
      deployment: Partner
      source_chain: Nexus
      source_token: AP3X
      destination_chain: Prime
      destination_token: AP3X
      amount: 2
      eternl_wallet_extension: Stable
      notification: false
    secrets:
      SIGN_KEY: ${{ secrets.SIGN_KEY }}
      NEXUS_WALLET_RECOVERY_PHRASE: ${{ secrets.NEXUS_WALLET_RECOVERY_PHRASE }}
      PRIME_WALLET_RECOVERY_PHRASE: ${{ secrets.PRIME_WALLET_RECOVERY_PHRASE }}
  inputs_check:
    name: Inputs Check
    runs-on: ubuntu-latest
    outputs:
      notification: ${{ steps.notification_input_check.outputs.notification }}
    steps:
      - name: Notification Input Check
        id: notification_input_check
        run: |
          if [[ "${{ inputs.notification }}" != "false" ]]; then
            echo "notification=true" >> $GITHUB_OUTPUT
          else
            echo "notification=false" >> $GITHUB_OUTPUT
          fi
  notification:
    name: Send notification
    uses: ./.github/workflows/multiple-transactions-without-vector-notification.yml
    needs: [prime_to_nexus, nexus_to_prime, inputs_check]
    if: ((success() || failure()) && (needs.inputs_check.outputs.notification == 'true'))
    with:
      bridge: Reactor
      deployment: Partner
      eternl_wallet_extension: Stable
      tx_01_source_chain: Prime
      tx_01_source_token: AP3X
      tx_01_destination_chain: Nexus
      tx_01_destination_token: AP3X
      tx_01_source_status: ${{ needs.prime_to_nexus.outputs.source_status }}
      tx_01_bridge_status: ${{ needs.prime_to_nexus.outputs.bridge_status }}
      tx_01_destination_status: ${{ needs.prime_to_nexus.outputs.destination_status }}
      tx_01_status: ${{ needs.prime_to_nexus.outputs.bridging_status }}
      tx_02_source_chain: Nexus
      tx_02_source_token: AP3X
      tx_02_destination_chain: Prime
      tx_02_destination_token: AP3X
      tx_02_source_status: ${{ needs.nexus_to_prime.outputs.source_status }}
      tx_02_bridge_status: ${{ needs.nexus_to_prime.outputs.bridge_status }}
      tx_02_destination_status: ${{ needs.nexus_to_prime.outputs.destination_status }}
      tx_02_status: ${{ needs.nexus_to_prime.outputs.bridging_status }}
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
