---
name: Source Katana Transactions

on:
  workflow_call:
    inputs:
      bridge_name:
        type: string
        required: true

    outputs:
      transactions:
        value: ${{ jobs.collect.outputs.transactions }}

permissions:
  id-token: write
  contents: read
  security-events: write

jobs:
  katana_eth_to_preview_ckatanaeth:
    name: Katana(ETH) to Preview(cKatanaETH)
    uses: ./.github/workflows/single-transaction.yml
    if: ${{ inputs.bridge_name == 'Skyline' }}
    with:
      bridge_name: ${{ inputs.bridge_name }}
      source_chain: Katana
      amount: 0.0005
      source_token: ETH
      destination_chain: Preview
      destination_token: cKatanaETH
      notification: false
    secrets: inherit

  collect:
    name: Collect
    uses: ./.github/workflows/collect-transactions.yml
    if: (!cancelled())
    needs:
      - katana_eth_to_preview_ckatanaeth
    with:
      needs_json: ${{ toJSON(needs) }}
      source_chain: Katana
